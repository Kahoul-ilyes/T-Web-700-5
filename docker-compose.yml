version: '3'
services:
  backend:
    container_name: t-web-backend
    build: ./backend
    restart: always
    ports:
      - "3000:3000"

  frontend:
    build: ./frontend
    image: t-web-frontend
    restart: always
    depends_on:
      - backend
    ports:
      - "80:80"
      - "443:443"
    environment:
      - VIRTUAL_HOST=35.214.83.191

  nginx-proxy:
    image: jwilder/nginx-proxy
    ports:
      - "80:80"
    volumes:
      - /var/run/docker.sock:/tmp/docker.sock:ro
